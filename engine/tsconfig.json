{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "rootDir": "./src",
    "outDir": "./dist",
    "lib": ["ES2022", "DOM", "DOM.Iterable"]
  },
  "include": ["src"] // ignores everything except the files under src. Useful if you want to run tests outside src
}